# 개발자 A 작업 문서

## 문서 정보
- 작성일: 2026-03-07
- 기준 문서: [구현 계획](/Users/erickwon/workspace/personal/bgm-canvas/docs/2026-03-07-bgm-canvas-implementation-plan.md)
- 역할: 캔버스 입력, 시각 UI, 온보딩, 사용자 상호작용 중심 구현

## 1. 작업 원칙
- 작업 시작 전 최신 `develop`을 반영한다.
- 구현 전 테스트를 먼저 작성한다.
- 공용 타입 변경은 개발자 B와 먼저 합의한다.
- 각 작업은 로컬 브라우저에서 직접 확인한다.

## 2. 작업 시작 루틴
```bash
git fetch origin
git pull --rebase origin develop
npm install
npm run test
```

## 3. 담당 범위
- Vite/React 앱 기본 UI
- 캔버스 렌더링
- pointer 기반 드로잉 입력
- 검은색 단일 선 UI
- undo/reset/erase 상호작용
- scene/motif 상태 표시 UI
- 재생/일시정지/온보딩 UI
- 수동 브라우저 QA

## 4. 선행 합의 필요 항목
- `Stroke`, `SceneElement`, `AudioLayer` 타입
- `unknown` motif 처리 방식
- undo 시 UI 반영 규칙
- 활성 레이어 표시 형식

## 5. 단계별 상세 할 일
### 단계 A-0. 앱 뼈대와 캔버스 자리 만들기
- [ ] Vite React TS 앱 구조를 정리한다.
- [ ] 메인 화면에 캔버스 영역과 컨트롤 영역 레이아웃을 만든다.
- [ ] 빈 상태 smoke UI를 만든다.
- [ ] 로컬 브라우저에서 첫 화면이 뜨는지 확인한다.

TDD:
- [ ] `App` 렌더 테스트 작성
- [ ] 캔버스 placeholder 노출 테스트 작성

완료 기준:
- [ ] `npm run dev`로 빈 캔버스가 보인다.
- [ ] 기본 렌더 테스트가 통과한다.

### 단계 A-1. 검은색 단일 선 드로잉 UI
- [ ] canvas pointer down/move/up 이벤트를 연결한다.
- [ ] stroke 렌더링을 구현한다.
- [ ] 선 색상을 검은색으로 고정한다.
- [ ] 색상 팔레트나 브러시 선택 UI를 만들지 않는다.

TDD:
- [ ] 드로잉 후 stroke 개수 증가 테스트 작성
- [ ] 검은색 단일 선 스타일 유지 테스트 작성

완료 기준:
- [ ] 마우스로 선이 그려진다.
- [ ] 터치 입력도 기본 동작한다.
- [ ] 다른 색상 선택 UI가 없다.

브라우저 검증:
- [ ] 실제 브라우저에서 3개의 선을 그린다.
- [ ] 새로고침 전까지 화면에 유지되는지 확인한다.

### 단계 A-2. undo/reset/erase UI
- [ ] undo 버튼을 만든다.
- [ ] reset 버튼을 만든다.
- [ ] erase 방식은 MVP 기준으로 `최근 stroke undo` 우선 구현한다.
- [ ] 상태가 비어 있으면 버튼 비활성화 규칙을 적용한다.

TDD:
- [ ] undo 클릭 시 마지막 stroke 제거 테스트 작성
- [ ] reset 클릭 시 화면 초기화 테스트 작성
- [ ] 빈 상태 버튼 비활성화 테스트 작성

완료 기준:
- [ ] 사용자가 실수한 선을 쉽게 되돌릴 수 있다.
- [ ] reset으로 캔버스가 완전히 초기화된다.

브라우저 검증:
- [ ] 선 3개를 그리고 undo 1회, reset 1회 실행

### 단계 A-3. motif 및 오디오 상태 UI
- [ ] 현재 인식된 motif badge 영역을 만든다.
- [ ] 현재 활성 오디오 레이어 수를 보여준다.
- [ ] 오디오 상태 텍스트 `idle/loading/playing/stopped`를 노출한다.
- [ ] `unknown` 입력 시에도 사용자에게 부드럽게 표시한다.

TDD:
- [ ] motif badge 렌더 테스트 작성
- [ ] active layer count 표시 테스트 작성

완료 기준:
- [ ] scene 해석 결과가 UI에 반영된다.
- [ ] 오디오 상태를 사용자가 볼 수 있다.

의존성:
- 개발자 B의 scene/audio 상태 계약 필요

### 단계 A-4. 세션 컨트롤과 온보딩
- [ ] 재생/일시정지 버튼 UI를 만든다.
- [ ] 첫 진입 안내 문구를 최소 텍스트로 넣는다.
- [ ] 학생/성인 모두 바로 이해 가능한 설명으로 제한한다.
- [ ] 접근성 기본 속성(`aria-label`, button role)을 넣는다.

TDD:
- [ ] pause/replay 버튼 상호작용 테스트 작성
- [ ] 온보딩 문구 노출 테스트 작성

완료 기준:
- [ ] 사용자가 설명 없이 첫 그림을 시작할 수 있다.
- [ ] 재생/일시정지가 UI에서 제어된다.

브라우저 검증:
- [ ] 앱 진입 후 5초 안에 첫 그림을 시작할 수 있는지 확인

### 단계 A-5. 수동 QA와 폴리시
- [ ] 학생 사용자 시나리오를 직접 재현한다.
- [ ] 성인 사용자 시나리오를 직접 재현한다.
- [ ] 레이아웃 깨짐, 버튼 상태, 텍스트 과다 여부를 점검한다.
- [ ] 발견한 UI 버그를 수정한다.

완료 기준:
- [ ] 브라우저에서 캔버스, motif UI, controls가 함께 자연스럽게 동작한다.

## 6. 개발자 B와의 인터페이스
### A가 제공해야 하는 것
- 캔버스에서 생성한 stroke 데이터 구조
- undo/reset 이후 최신 stroke 집합
- 현재 선택/삭제 이벤트

### B로부터 받아야 하는 것
- stroke -> motif 해석 결과
- motif -> active audio layer 상태
- audio control 이벤트 결과

## 7. 통합 전 체크리스트
- [ ] 최신 `develop` 동기화 완료
- [ ] `npm run test` 통과
- [ ] `npm run dev`로 로컬 브라우저 확인
- [ ] 상대 개발자 변경 사항 반영 후 UI 깨짐 없음

## 8. 브라우저 직접 검증 시나리오
### 학생 사용자
1. 홈 화면 진입
2. 캠프파이어 그림
3. 별 추가
4. undo 실행
5. 다시 별 추가

### 성인 사용자
1. 홈 화면 진입
2. 비, 창문, 책상 순서로 그림
3. 현재 활성 motif 확인
4. pause
5. replay
6. reset

## 9. 완료 보고 기준
- 어떤 화면/상호작용을 구현했는지
- 어떤 테스트를 추가했는지
- 브라우저에서 무엇을 직접 확인했는지
- 개발자 B가 이어서 통합할 때 알아야 할 계약 변경이 있는지
